// history dependency from react router dom
//
import { createBrowserHistory } from "history"
// function we call to create a history object
export default createBrowserHistory()
// allows us to create a history object so we can navigate user around app
